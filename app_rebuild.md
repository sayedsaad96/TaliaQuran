# 🕌 Talia Quran (تالية) - Product Requirements Document (PRD) للمشروع الجديد

هذه الوثيقة موجهة لمهندس البرمجيات / قائد الفريق التقني بهدف **بناء تطبيق "تالية" من الصفر** بناءً على أفضل الممارسات الحديثة. 
توفر هذه الوثيقة شرحاً كاملاً للفكرة، الأهداف، والميزات المطلوبة (Business & Product Requirements)، مع ترك الحرية التقنية للمطور لاختيار الهيكلية والأدوات الأنسب (Tech Stack & Architecture) لتحقيق هذه المتطلبات بأعلى جودة ممكنة.

---

## 1. 🌟 الرؤية العامة والهدف (Product Vision)
**"تالية"** هو تطبيق قرآني ذكي ومتميز يهدف إلى إحداث ثورة في طريقة حفظ ومراجعة القرآن الكريم. يدمج التطبيق بين أصالة القراءة (المصحف العثماني) والتكنولوجيا الحديثة (الذكاء الاصطناعي، خوارزميات التكرار المتباعد، والتلعيب Gamification) ليقدم تجربة مخصصة للأطفال والكبار على حد سواء.

### المبدأ الأساسي (Core Principle): Offline-First
يجب أن يعمل التطبيق بكامل طاقته (عرض المصحف، التلاوة الصوتية، الحفظ، التسميع الصوتي، تقدم المستخدم، والأذكار) **بدون الحاجة لاتصال بالإنترنت**. يتم استخدام السحابة (Cloud/Backend) فقط لمزامنة البيانات وربط الحسابات (مثل ربط حساب الطفل بالوالد) عندما يتوفر الاتصال.

---

## 2. 👥 الشرائح المستهدفة (User Personas)
التطبيق يخدم 3 فئات رئيسية، لكل منها واجهة وتجربة مستخدم (UX) مخصصة:
1. **المستخدم البالغ (Adult):** يبحث عن خطة حفظ ذكية، واجهة هادئة خالية من المشتتات، وتقارير تقدم تفصيلية (Heatmaps & Streaks).
2. **الطفل (Kid):** يحتاج إلى تجربة مليئة بالتحديات (Gamification)، واجهة بصرية جذابة، مستويات، وألعاب لتحفيزه على الاستمرار.
3. **الولي/المشرف (Guardian):** يحتاج إلى لوحة تحكم (Dashboard) لمتابعة تقدم الطفل، نسبة إنجازه، ونتائج اختباراته عن بُعد.

---

## 3. ✨ الميزات الأساسية المطلوبة (Core Features)

### 3.1 تجربة المصحف الممتازة (Premium Mushaf Experience)
* **عرض دقيق للمصحف (شرط إلزامي):** يجب استخدام مكتبة `qcf_quran_plus` حصرياً لعرض صفحات المصحف بالرسم العثماني الموثق، لضمان جودة فائقة (Vector-based) بدلاً من الصور التقليدية، ولتمكين التفاعل البرمجي الدقيق مع كل آية على حدة.
* **التلاوة الصوتية:** استماع آية بآية مع تظليل الآية المقروءة، وتوفير إمكانية تحميل المقاطع الصوتية للاستماع إليها بدون إنترنت (Offline Caching).
* **إمكانيات مخصصة:** وضع التركيز (Focus Mode) لإخفاء القوائم، التحكم بحجم الخط، وعلامات مرجعية دائمة.

### 3.2 محرك الحفظ والمراجعة (Memorization & Review Engine)
هذا هو القلب النابض للتطبيق. يجب أن تمر جلسة الحفظ بالمراحل التالية بشكل متسلسل:
1. **التعلم (Learning):** قراءة واستماع الآيات الجديدة.
2. **محاولة الحفظ (Memorizing):** إخفاء وإظهار الكلمات للتدرب.
3. **التسميع (Recitation):** اختفاء النص بالكامل، ويقوم المستخدم بقراءة الآية بصوته.
4. **التحقق الصوتي (AI Voice Verification):** استخدام الذكاء الاصطناعي (Speech-to-Text) للاستماع لتلاوة المستخدم ومطابقتها بالنص القرآني لتحديد الأخطاء (Pass/Fail).
5. **نظام التلميحات (Hint System):** في حال نسيان المستخدم، يمكنه طلب تلميح (كلمة واحدة، أو الآية كاملة)، مما يؤثر على تقييمه النهائي.
6. **مراجعة المقطع (Block Review):** اختبار شامل للمقطع المحفوظ دون أي تلميحات للتأكد من إتقانه قبل اعتماده.

### 3.3 خوارزمية التكرار المتباعد (Spaced Repetition System - SRS)
* بناء خوارزمية ذكية تحدد موعد المراجعة القادمة للآيات بناءً على قوة حفظ المستخدم (مثلاً: المراجعة بعد 1، 3، 7، 14، 30، ثم 90 يوماً).
* الهدف هو نقل الآيات من الذاكرة قصيرة المدى إلى الذاكرة طويلة المدى.

### 3.4 الموجه الذكي للخطة اليومية (Smart Coach)
* **توليد خطة مخصصة:** محرك ذكي يقوم ببناء خطة يومية (Daily Plan) للمستخدم البالغ، تحدد له بالضبط ما يجب عليه حفظه الجديد وما يجب عليه مراجعته اليوم.
* **التكيف مع أداء المستخدم:** يقوم الموجه الذكي بتعديل كمية الحفظ والمراجعة بناءً على وتيرة المستخدم ومدى التزامه بالخطة.
* **واجهة مهمتك التالية (Next Mission):** يستبدل نظام اختيار السور اليدوي بواجهة بسيطة تقترح على المستخدم مهمته التالية مباشرة لتجنب التشتت.

### 3.5 نظام التلعيب والتقدم (Gamification & Progress)
* **المسار اليومي (Streaks):** تشجيع المستخدم على فتح التطبيق يومياً.
* **نقاط الخبرة (XP):** منح نقاط بناءً على كمية الحفظ والمراجعة والأذكار.
* **الشارات والشهادات (Badges & Certificates):** توليد شهادات إنجاز بصيغة PDF قابلة للطباعة عند إتمام أجزاء معينة.
* **الخريطة التفاعلية للأطفال:** عرض المنهج على شكل خريطة مراحل أو جزر لزيادة حماس الطفل.

### 3.6 نظام المراقبة الأبوية (Guardian Linking System)
* إمكانية ربط جهاز الطفل بجهاز الوالد (مثلاً عبر مسح QR Code أو إرسال كود ربط).
* مزامنة البيانات عبر خادم سحابي بحيث يرى الوالد في جهازه: ماذا حفظ الطفل اليوم؟ ما هي أخطاؤه؟ وكم عدد نقاطه؟

### 3.7 أذكار المسلم الذكية (Smart Azkar)
* عدادات تفاعلية للأذكار مع دعم الاهتزاز (Haptic Feedback) عند الضغط.
* نظام تنبيهات محلية (Local Notifications) يذكر المستخدم بأذكار الصباح والمساء والدعاء اليومي حسب إعداداته.

---

## 4. ⚙️ المتطلبات والقيود التقنية (Technical Constraints & Guidelines)

بينما نترك للمطور حرية اختيار الأدوات، إلا أن هذه الشروط البرمجية والتصميمية **إلزامية** لضمان جودة المنتج:

1. **الاستقلالية عن الإنترنت (Offline-First Architecture):**
   - يجب أن تُحفظ كافة بيانات المستخدم (تقدم، أخطاء، تقييمات، أذكار) في قاعدة بيانات محلية سريعة جداً.
   - تتم مزامنة البيانات مع الخادم (Sync) في الخلفية متى ما توفر اتصال بالإنترنت، مع معالجة تعارض البيانات (Conflict Resolution) إن وجدت.

2. **بنية معمارية نظيفة وقابلة للتوسع (Clean & Scalable Architecture):**
   - يجب فصل واجهة المستخدم (UI) عن منطق الأعمال (Business Logic) وعن طبقة البيانات (Data Layer).
   - تصميم النظام على شكل وحدات مستقلة (Modularization) بحيث يمكن تطوير ميزة الحفظ بشكل منفصل عن ميزة الأذكار.

3. **جودة الواجهات والتجربة (Premium UI/UX):**
   - تصميم واجهات عصرية جداً، استخدام الـ Micro-animations لدعم تفاعل المستخدم.
   - دعم كامل وسلس للغة العربية (RTL) دون أي أخطاء في العرض.

4. **الذكاء الاصطناعي ومعالجة الصوت (Audio & AI Processing):**
   - قدرة التطبيق على إدارة الصوت بكفاءة عالية دون التسبب في بطء الواجهة.
   - تكامل سلس مع خدمات تحويل الصوت إلى نص (Speech-to-Text) لمطابقة التلاوة.

---

## 5. 🚀 ملخص للمطور الجديد
لديك الحرية الكاملة في اختيار التقنيات (مثال: إذا كنت تستخدم Flutter، يمكنك اختيار Bloc أو Riverpod، Isar أو Drift، Supabase أو Firebase). 
الهدف هو: **إعادة هندسة التطبيق ليتحمل مئات الآلاف من المستخدمين بكفاءة، مع التركيز الشديد على تجربة المستخدم، وعمل التطبيق كاملاً في وضع عدم الاتصال (Offline).**
